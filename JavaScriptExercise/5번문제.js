// 이 함수는 수정하지 마세요.
function double(argument) {
  return argument * 2;
}

function invoke() {
  let result;
  // 위의 double이라는 함수를 호출하고, 인자로 숫자 7을 넣은 뒤, 결과값을 result 변수에 할당하세요.
  // 여기에 코드를 작성하세요.
  result = double(7);
  // 아래 코드는 수정하지 마세요.
  return result;
}

console.log(invoke());
